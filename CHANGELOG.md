# Changelog

## 2026-04-22
* Backend: Updated `/api/v1/updates` endpoint to return `project_currency` from the `Project` relation.
* Frontend: Refactored `MeetingLogs.tsx` to dynamically fetch the correct project currency sign dynamically instead of defaulting to USD.
